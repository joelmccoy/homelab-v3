title = "homelab-v3 gitleaks config"

[extend]
useDefault = true

# Sealed-secret encrypted blobs are safe to commit; they're already encrypted
# with the in-cluster sealed-secrets controller's public key. The high-entropy
# base64 trips generic-api-key.
[allowlist]
paths = [
  '''.*-sealed-secret\.yaml$''',
]
