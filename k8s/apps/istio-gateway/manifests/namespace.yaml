apiVersion: v1
kind: Namespace
metadata:
  name: istio-ingress
  annotations:
    argocd.argoproj.io/sync-wave: "-100"
  labels:
    # Gateway pod runs in this namespace; mesh it for L4 mTLS.
    istio.io/dataplane-mode: ambient
