# istiod: Istio control plane, Ambient profile.

hub: docker.io/istio
# renovate: datasource=docker depName=docker.io/istio/pilot versioning=docker
tag: 1.29.2
variant: distroless

profile: ambient

pilot:
  env:
    PILOT_ENABLE_AMBIENT: "true"

meshConfig:
  accessLogFile: /dev/stdout
