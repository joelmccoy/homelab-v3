apiVersion: v1
kind: Service
metadata:
  name: kaneo
  namespace: kaneo
  labels:
    app.kubernetes.io/name: kaneo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kaneo
  ports:
    - name: http
      port: 5173
      targetPort: http
